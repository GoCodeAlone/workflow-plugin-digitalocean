# Phase 2.5 — DO v1.3.0 FinalizeApply smoke validation

**Date:** 2026-05-17
**Cascade:** workflow#695 Phase 2.5
**Shipped artifacts:**
- workflow v0.55.0 (squash sha `aac519da`; GoReleaser run 25985943298; 18 assets; latest)
- workflow-plugin-digitalocean v1.3.0 (squash sha `d8a203fd`; GoReleaser run 25987516580; 5 assets; latest)
- workflow-plugin-digitalocean sync chore-PR auto-fired reliably via post-aws#18 defensive `workflow_dispatch` + tag regex validation; no-op'd cleanly (plugin.json already at 1.3.0 in merged PR)

## Smoke verification

### Surface validation (executed as part of cascade closeout)

| Check | Method | Result |
|---|---|---|
| Plugin tag published | `gh api repos/GoCodeAlone/workflow-plugin-digitalocean/releases/tags/v1.3.0` | `draft=false`, `n_assets=5`, marked latest |
| Workflow tag published | `gh api repos/GoCodeAlone/workflow/releases/tags/v0.55.0` | `draft=false`, `n_assets=18`, marked latest |
| go.mod pin upgrade verified | DO v1.3.0 PR #123 merged with `github.com/GoCodeAlone/workflow v0.55.0` in go.mod | confirmed via gh pr view |
| ComputePlanVersion="v2" declared | DO v1.3.0 PR #123 `internal/iacserver.go` Capabilities return literal | confirmed via plan-review + code-reviewer |
| IaCProviderFinalizer service auto-registered | DO v1.3.0 implements `pb.IaCProviderFinalizerServer` interface (compile-time assert at `_ pb.IaCProviderFinalizerServer = (*doIaCServer)(nil)`); SDK auto-reg from workflow v0.55.0 picks it up | wftest regression-gate `TestIaCServiceChecks_CoversEveryProtoService` passing |
| FinalizeApply per-driver iteration semantically equivalent to v1 wrapper | Task 8 regression test `TestDOIaCServer_FinalizeApply_FlushesDeferredUpdates` exercises end-to-end gRPC handler with seeded deferred trusted_sources; asserts `UpdateFirewallRules` called with `{type:app, value:<UUID>}` rule shape | PASS race-clean |
| Per-driver error attribution preserved | Task 8 regression test `TestDOIaCServer_FinalizeApply_PreservesPerDriverErrorAttribution` injects `minimalDBMock.flushErr`; asserts resp.Errors[0] has `Resource="infra.database"` + `Action="deferred_update"` + sentinel string preserved | PASS race-clean |
| v1 semantic preservation (finalize skipped on outer err) | workflow v0.55.0 engine test `TestApplyPlanWithHooks_OnPlanComplete_SkippedOnOuterError` asserts deferred closure's `if err != nil { return }` gate matches DOProvider.Apply v1 wrapper's "return without flushing on top-level err" behavior at provider.go:276-282 | PASS race-clean |

### Live cloud smoke — DEFERRED to next BMW deploy

The plan-mandated runtime-launch-validation per cycle-1 plan-review I-5 specified:

> install DO v1.3.0 against wfctl v0.55.0, run representative DB+app plan against DO project, verify database `trusted_sources` firewall rules applied post-app-create (= deferred-flush ran under v2 dispatch via FinalizeApply RPC)

Live execution requires:
- DigitalOcean API credentials with project provisioning rights
- A representative DB+app YAML config exercising deferred-trusted_sources
- Real cloud apply (costs $; not safely run from automation context without explicit operator authorization)

**Decision per Phase 2 cascade precedent:** matching the Phase 2 closeout pattern (cross-plugin smoke for v1.2.0 cascade was operator-run-only, NOT CI-gated, deferred to next BMW deploy), live cloud smoke for v1.3.0 is deferred to the next BMW (buymywishlist) deploy that uses workflow v0.55.0 + DO v1.3.0. BMW deploys against DigitalOcean App Platform + Managed Database with `trusted_sources` referencing App Platform IDs — the canonical regression vector for this cascade.

If BMW deploy reveals regression: per Phase 2.5 plan §Rollback, cut DO v1.3.1 reverting v2 declaration + FinalizeApply impl + restoring v1.2.0 state. DO consumers continue on workflow v0.55.0 + DO v1.2.0 (v1-dispatched, deferred-flush still runs via DOProvider.Apply wrapper).

### Test coverage matrix (locked Phase 2.5 invariants)

| Layer | Test | Repo |
|---|---|---|
| Engine deferred-closure all-exit-path coverage | `TestApplyPlanWithHooks_OnPlanComplete_*` (5 tests) | workflow |
| SDK auto-registration | `TestRegisterIaCServices_AutoRegistersFinalizer` + `AllOptionals_AllRegistered` | workflow |
| Adapter accessor wiring | `TestTypedAdapter_Finalizer_PopulatedWhenRegistered` + `_NilWhenNotRegistered` | workflow |
| wfctl-side OnPlanComplete closure 6-branch coverage | `TestStatePersistenceHooks_OnPlanComplete_*` (6 branches: no adapter, nil finalizer, gRPC err, no resp errors, with resp errors, format invariant) | workflow |
| gRPC service registration regression gate | `TestIaCServiceChecks_CoversEveryProtoService` | workflow |
| DO FinalizeApply end-to-end | `TestDOIaCServer_FinalizeApply_FlushesDeferredUpdates` | workflow-plugin-digitalocean |
| DO per-driver error attribution | `TestDOIaCServer_FinalizeApply_PreservesPerDriverErrorAttribution` | workflow-plugin-digitalocean |
| DO Capabilities v2 declaration | `TestDOIaCServer_Capabilities_DeclaresV2` (T8 follow-up) | workflow-plugin-digitalocean |

## Cascade completion

Phase 2.5 cascade ✅ SHIPPED 2026-05-17. workflow#695 closed. Tracking for follow-ups:
- Phase 2.3 (compensation enum tags 4+5 reserved; tag 6 candidate SKIPPED)
- Phase 3 (delete `DOProvider.Apply` v1 wrapper as dead code — eliminates the FinalizeApply-vs-Apply duplication Copilot flagged on PR #123)
- Per-plugin ComputePlanVersion regression-guard test cluster (aws/gcp/azure/DO)
